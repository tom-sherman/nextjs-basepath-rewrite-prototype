Requires Node, pnpm, and Deno

- `pnpm i`
- `cd legacy-app && pnpm run preview` To run the "Legay" app, a vite SPA
- `cd next-app && pnpm run build && pnpm run start` To run the Next.js app
- `cd proxy && pnpm run start`
